#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

mkdir -p r_libs
R_LIBS="`pwd`/r_libs" R -e "install.packages('Rserve', repos='http://cran.r-project.org'); library(Rserve); Rserve()"

gem install bundler
bundle install

# Do any other automated setup that you need to do here
